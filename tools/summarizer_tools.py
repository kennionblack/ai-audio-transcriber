def extract_key_points(text: str, max_points: int) -> str:
    """
    Extract key points from the given text.
    """
    print(f"---- Extracting key points (max {max_points}) ----")
    return f"key points (max {max_points})"
