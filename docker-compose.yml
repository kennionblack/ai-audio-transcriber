services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/workspace
      - ./audio:/workspace/audio  # host audio files mapped inside container
    stdin_open: true
    tty: true
